%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followees.each do |followee|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(followee.email.downcase)}?s=40")
            = link_to followee.username
          %td.extra-padding #{followee.videos.count}
          %td.extra-padding #{followee.followees.count}
          %td.extra-padding
            = link_to user_person_path(user_id: current_user.id, id: followee.id), method: :delete do
              %i.glyphicon.glyphicon-remove
